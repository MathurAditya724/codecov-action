minVersion: "2.21.2"
changelog:
  policy: auto
artifactProvider:
  name: github
  config:
    artifacts:
      Build & Test:
        - action-package
        - gh-pages
targets:
  - name: github
  - name: gh-pages
